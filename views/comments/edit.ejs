<% include ../partials/header %>

<div class="container">
    <div class="row">
        <h1 style="text-align: center;">Edit Comment for <%=camp.name%></h1>
        <div style="width: 30%; margin: 40px auto; ">
            <form method="POST" action="/camps/<%=camp._id%>/comments/<%=comment.id%>?_method=PUT">
                <div class="form-group">
                    <textarea class="form-control" type="text" placeholder="Comment" name="comment[text]">
                        <%- comment.text %>
                    </textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit!</button>
                </div>
            </form>
            <a href="/">Go back</a>
        </div>
    </div>
</div>

<% include ../partials/footer %>